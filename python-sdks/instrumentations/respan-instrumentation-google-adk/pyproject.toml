[tool.poetry]
name = "respan-instrumentation-google-adk"
version = "0.1.0"
description = "Respan instrumentation plugin for Google ADK"
authors = ["Respan <team@respan.ai>"]
license = "Apache 2.0"
readme = "README.md"
packages = [
    { include = "respan_instrumentation_google_adk", from = "./src" },
]

[tool.poetry.dependencies]
python = ">=3.11,<3.14"
respan-tracing = "^2.16.1"
respan-instrumentation-openinference = ">=1.2.2"
google-adk = ">=1.5.0"
openinference-instrumentation-google-adk = ">=0.1.12"
packaging = ">=21.0"

[tool.poetry.plugins."respan.instrumentations"]
google-adk = "respan_instrumentation_google_adk:GoogleADKInstrumentor"

[tool.pytest.ini_options]
markers = [
    "integration: live network tests (requires API keys)",
]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
